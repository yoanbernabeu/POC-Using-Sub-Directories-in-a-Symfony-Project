{% extends 'base.html.twig' %}

{% block title %}Edit Back{% endblock %}

{% block body %}
    <h1>Edit Back</h1>

    {{ include('back/back/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('back_back_index') }}">back to list</a>

    {{ include('back/back/_delete_form.html.twig') }}
{% endblock %}
